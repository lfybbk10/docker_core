apiVersion: v1
kind: Service
metadata:
  name: cicd-app
  labels:
    app: cicd-app
spec:
  selector:
    app: cicd-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP

